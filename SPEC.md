# SPEC.md — 撥水ガラス雨滴ライブ壁紙「Rain on Glass · 雨粒」完成仕様書

> **この文書の使い方**
> これは実装AIに投げるための仕様書である。§7 のタスク指令はそれぞれ自己完結しており、
> 1タスクずつコピーして実装AIに渡せば作業できる粒度になっている。
> 実装AIに渡すときは必ず「§0 大目標」「§4 注目表」と該当タスクをセットで渡すこと。
> 対象ファイルは原則 `index.html` 1ファイル（シングルファイルアプリ）。ビルド工程なし。

---

## §0. 大目標（North Star）

**「撥水コートされたガラスに雨が降っている様子を、見ているだけで気持ちいい画面オブジェとしてスマホのライブ壁紙にする」**

判定基準（これに全部YESなら完成）：

1. **水に見えるか** — 初見の人に5秒見せて「これ何？」と聞いたとき「ガラスについた雨」と答えるか。
   「青い丸」「エフェクト」「ゲームの画面」と答えたら不合格。
2. **撥水に見えるか** — 水滴が「ベタッと張り付いている」のではなく「球に近い形で乗っている」
   「転がるとき道がスッと綺麗になる」と感じられるか。
3. **ずっと見ていられるか** — 3分間眺めて飽きないか。次にどの粒が落ちるかを目で追いたくなるか。
4. **軽いか** — ミドルレンジスマホで60fps、画面OFF時は消費ゼロ、1時間つけっぱなしで熱くならない。

**やらないこと（アンチゴール）**：
- 水滴を「水色の塗りつぶし」で表現すること。実物の水滴は無色透明で、見えるのは背景の屈折像・暗いリム・白いハイライトだけである。
- 派手なパーティクル演出、発光、ブルーム。水は光らない。
- 物理的に正しくても見た目が気持ちよくない挙動の採用（見た目 > 物理の忠実さ。ただし物理は見た目の根拠として使う）。

---

## §1. 現状（v0 ベースライン）

リポジトリ: `kuboshige/live-waterdrop` / デプロイ先: `https://kuboshige.github.io/live-waterdrop/`（gh-pages ブランチ）

`index.html` に実装済みのもの：

| 機能 | 実装状況 | 課題 |
|---|---|---|
| Canvas 2D 二層構成（メイン `#c` + 小滴レイヤー `#small`） | ✅ | 小滴が単色塗りで水に見えない |
| 屈折（clip + `scale(-1.65)` の反転拡大背景） | ✅ | 一様スケールで縁の歪みがない。中心も縁も同じ倍率 |
| 暗リム・スペキュラ・エッジ暗化 | ✅ | 静的。光源方向の概念がない |
| 体積保存合体（`cbrt(r1³+r2³)`）・r⁵重心 | ✅ | 合体時の振動（ぷるぷる）がない |
| 空間グリッド衝突 O(n) | ✅ | — |
| ピン止め（pinExtra）・スティックスリップ | ✅ | ヒステリシスの物理表現が簡略 |
| 落下時の扁平変形（楕円） | ✅ | 変形方向が水平のみ。実物は進行方向に伸びる |
| 落下痕（かまぼこ水柱 + 上端から一定速度で消費 + ビーズ残留） | ✅ | 撥水面では本来「痕がほぼ残らない」のが気持ちよさの源泉。方向転換が必要 |
| 雨条・ガラス上部シャイン・ビネット | ✅ | — |
| 設定パネル（サイズ範囲・降雨強度・背景画像） | ✅ | — |
| Page Visibility で画面OFF時停止 | ✅ | — |

**v0の最大の問題**: 小滴（r < 5.5px）が「水色の円」で描かれており、画面の大半を占める小滴が水に見えない。ここが §0-1 の不合格要因。

---

## §2. 物理分析 — 実装の根拠となる数値と現象

実装AIはここの数値を実装のパラメータ根拠として使うこと。数式をそのまま実装する必要はなく、「見た目がこうなる理由」として参照する。

### §2.1 濡れ性（撥水とは何か）

- **接触角 θ**: 水滴とガラス面のなす角。素ガラス（親水）は θ≈20–40°でベタッと広がる。
  撥水コート（ガラコ/Rain-X等のシリコーン・フッ素系）は **θ≈100–115°**。水滴は半球よりやや盛り上がった「球帽（spherical cap）」になる。
  → **描画指針**: 真上から見ると水滴の輪郭は円に近く、レンズとして厚い。横に広がった薄いパンケーキ形にしてはいけない。
- **接触角ヒステリシス Δθ = θ_advancing − θ_receding**: 前進角と後退角の差。撥水コートは Δθ が小さい（10–20°）。
  Δθ が小さい＝**小さい傾きでもコロッと転がり出し、転がった跡に水膜を残さない**。これが撥水の気持ちよさの物理的正体。
- **転落条件（Furmidge の関係）**: `m·g·sinα > γ·w·(cosθ_r − cosθ_a)`。
  質量（∝r³）が大きくなるほど左辺が急速に勝つ → **臨界サイズを超えた瞬間、急に走り出す**。
  → **描画指針**: 「じわじわ動き出す」のではなく「限界を超えた瞬間ストンと発進」。発進時の初速ジャーク（現行実装の release jerk）は正しい方向性。
- **毛細管長 κ⁻¹ = √(γ/ρg) ≈ 2.7mm**: これより小さい水滴は表面張力支配で真球帽を保ち、
  大きい水滴は重力で潰れて扁平になる。70mm幅スマホ画面 ≈ 5.6px/mm として **約15px半径が「球帽↔扁平」の境界**。
  → **描画指針**: r < 15px はぷっくり球帽、それ以上は縦に潰れた形＋輪郭の揺らぎ。

### §2.2 光学（水滴はなぜ「水に見える」か）

水は無色。水滴が見える理由は以下の4要素**だけ**であり、これを全部やれば水に見える：

1. **屈折レンズ像（最重要）**: 屈折率 n_water=1.333。球帽水滴は平凸レンズで、焦点距離 f ≈ r/(n−1) ≈ 3r。
   背景がガラスのすぐ後ろ（焦点より遠い）にあるため、**水滴内には背景の倒立・広角圧縮像**が映る。
   - 中心部: ほぼ素通し〜軽い拡大
   - 縁に近づくほど: 急激に圧縮された広角像（魚眼効果）。**縁で歪みが最大**になるのが実物の特徴。
   - v0 の一様 `scale(-1.65)` は「中心も縁も同じ」なのでガラス玉っぽさが出ない。半径方向の非線形歪み（縁ほど強く圧縮）が必要。
2. **暗いリム**: 水滴の縁は視線に対して面が急角度になり、屈折光が視野外から来る＋全反射（水→空気の臨界角48.6°）で暗く落ちる。
   実物の写真では**輪郭に沿った幅 r×0.1〜0.15 の暗い縁取り**として現れる。v0 実装済みだが縁への集中が甘い。
3. **スペキュラハイライト**: 光源（空）の鏡面反射。Fresnel 反射率は正面入射で約2%と弱いが、光源が明るいので白く飛ぶ。
   - 特徴: **小さく・シャープで・光源方向に固定**。粒が動いてもハイライトは常に同じ側（通常は上寄り）。
   - にじんだ大きいグラデーションにするとゼリーに見える。小さくシャープに。
4. **接地影と集光（コースティクス）**: 水滴はレンズなので、透過光が水滴の「下（背景側）」に集まり、
   **水滴の周囲にうっすら暗い縁、真下〜中央に明るいスポット**を作る。背景が明るい時に効く。優先度は1–3より低い。

**色について**: 水滴自体に色を塗るのは最終手段。背景が暗い場合のみ、視認性のために極薄い明度補正を許可する（彩度を上げない・青くしない）。

### §2.3 動力学（気持ちよさの源）

- **合体振動（ぷるぷる）**: 2滴が合体すると表面張力で球に戻ろうとして減衰振動する。
  Rayleigh 固有振動数 f ∝ √(γ/ρR³) → **大きい粒ほどゆっくり・小さい粒ほど速く**震える。
  持続は実時間で0.1〜0.3秒程度。x方向とy方向が逆位相（縦に伸びたら横に縮む＝体積保存）。
  → これが「ぷるぷる」の正体で、快感要素として優先度が高い。
- **スティックスリップ**: 転落中もミクロなピン止め＆解放を繰り返し、カクッカクッと進む。v0実装済み。
  撥水面では滑走が支配的なので、大粒ほどスティック割合を下げる（v0の方向性は正しい）。
- **掃除効果（sweep）**: 走る大粒は経路上の小滴を食って加速し、**通り道が「拭いたように」綺麗になる**。
  撥水面の落下痕は親水面と違い**ほぼ残らない**。残るのは(a)経路脇に弾かれた微小サテライト滴、(b)大粒の尻から千切れた数個のビーズのみ。
  → v0の「かまぼこ水柱が残って上から消える」は親水ガラス寄りの表現。撥水表現に転換する（§7 P2-3）。
- **蛇行**: 走る水滴は完全な直線ではなく、ピン止めの不均一で±数px蛇行し、時々ハッキリ進路を変える（先にある大きめの滴に「引き寄せられて」合体するのが理由に見える）。
- **微小滴の成長**: 実物では画面上の微小滴は蒸発で消えるより「近隣と合体して成長する」方が支配的。
  静止小滴同士も接触すれば合体する。画面全体で「小滴がだんだん育ち、育ちきると落ちて掃除される」循環が回ると生態系っぽい飽きなさが出る。

### §2.4 「気持ちよさ」の心理側の分解

1. **予測と裏切りのリズム**: 「あの粒そろそろ落ちそう…落ちた！」の予測ゲームができること。限界近くの粒がわずかに揺れる・下に膨らむなどの「前兆」があると視線誘導になる。
2. **掃除のカタルシス**: 走った跡が綺麗になる。散らかる（雨が降り積もる）→片付く（大粒が走る）のループ。
3. **質量感**: 大粒はゆっくり発進して重々しく加速、小粒は軽くコロッと。全部同じ動きだと嘘くさい。
4. **音はなくてよい**（ライブ壁紙のため）。視覚だけで完結させる。

---

## §3. アーキテクチャ方針

### 二段構えで進める（重要な戦略判断）

- **トラックA（Canvas 2D 強化）**: 現行実装の延長。§7 の P1〜P2。
  Canvas 2D でも「非線形屈折の近似・ぷるぷる・撥水掃除」までは到達可能。**まずここで §0 判定基準の合格を狙う**。
- **トラックB（WebGL 移行）**: P3。ピクセル単位の真の屈折（法線マップ＋屈折シェーダ）、Fresnel、
  数千個の小滴の一括描画が可能になり、表現の天井が上がる。
  参考にすべき既存実装のアプローチ: 「水滴の法線マップをテクスチャに描き、フラグメントシェーダで背景テクスチャをUVオフセットサンプリングする」方式（codrops の Rain Effects で知られる手法）。
  **トラックAで合格してからやる。** WebGL化はリスク（実装コスト・デバイス互換・電池）があるため、A合格後の「更に上」として位置づける。

### 不変の制約（全タスク共通）

- `index.html` 1ファイル完結。外部ライブラリ・CDN禁止。ビルド工程禁止。
- スマホ縦画面が主戦場。タッチ操作前提。マウスは開発用フォールバック。
- `document.hidden` 時は一切の計算・描画をしない（実装済み。壊さないこと）。
- 既存の設定パネル（サイズ・降雨強度・背景変更）のUXを壊さない。
- 60fps目標。1フレームの JS 時間予算 8ms 以内（ミドルレンジ想定でデスクトップ計測値×2の余裕を見る）。
- コミットは機能単位。デプロイは gh-pages ブランチへ `index.html` を push。

---

## §4. 注目表（品質スコアカード）

各タスク完了時にこの表で自己採点し、対象項目が目標に達しているか確認すること。
「測り方」は基本的に目視スクリーンショット比較。P0-1 のデバッグ機能で計測できるものは数値で。

| # | 注目項目 | 目標状態 | 測り方 | 優先度 |
|---|---|---|---|---|
| Q1 | 屈折レンズ感 | 粒の中に背景の倒立像が見え、縁ほど強く歪む | 縞模様背景でスクショ。縞が粒の縁で圧縮されているか | ★★★ |
| Q2 | 暗リム | 輪郭に沿う幅 r×0.1 程度の暗い縁。内側はクリア | スクショ拡大 | ★★★ |
| Q3 | ハイライト | 小さくシャープ、全粒で同じ方向（上寄り） | スクショ。にじんだ大きな白グラデは不合格 | ★★★ |
| Q4 | 小滴の水感 | r<5px の粒も「青い点」でなく「歪みのある点」に見える | 縞背景スクショ | ★★★ |
| Q5 | ぷるぷる合体 | 合体直後 0.1–0.3s の減衰振動。大粒ほど遅い | 目視・スロー録画 | ★★★ |
| Q6 | 撥水掃除 | 大粒の通過後、経路が「拭いた」ように綺麗。残るのは微小サテライトのみ | 目視 | ★★★ |
| Q7 | 発進の質量感 | 臨界超えでストンと発進。大粒は重く加速 | 目視 | ★★☆ |
| Q8 | 蛇行 | 走行が完全直線でない。時々進路変更 | 目視 | ★★☆ |
| Q9 | 前兆 | 落ちる直前の粒がわずかに下に膨らむ/揺れる | 目視 | ★★☆ |
| Q10 | 小滴生態系 | 静止小滴同士の接触合体で徐々に育つ | 5分放置して観察 | ★★☆ |
| Q11 | 接地影・集光 | 明るい背景で粒の下に明スポット | スクショ | ★☆☆ |
| Q12 | パフォーマンス | 60fps、フレーム8ms以内、豪雨設定でも55fps以上 | P0-1 のFPS/msメーター | ★★★ |
| Q13 | 消費電力 | 画面OFF・バックグラウンドでCPU 0% | DevTools Performance で確認 | ★★★ |
| Q14 | 破綻がない | 粒の重なり・ちらつき・突然の出現/消滅がない | 3分観察 | ★★★ |

---

## §5. ロードマップ

```
P0 計測基盤        （半日）   スクショ比較とFPS計測ができる状態にする
 │
P1 光学リメイク    （2–3日）  Q1–Q4 合格。「水に見える」を達成する
 │
P2 動力学リメイク  （2–3日）  Q5–Q10 合格。「撥水に見える・飽きない」を達成する
 │
P3 WebGL化(任意)   （1週間）  表現の天井上げ。P1–P2合格後にのみ着手
 │
P4 仕上げ          （1–2日）  実機チューニング・電池・設定項目整理
```

依存関係: P0 → P1 → P2 → (P3) → P4。P1 内・P2 内の各タスクは番号順に実施。
**P1 完了時点と P2 完了時点で必ずユーザーレビュー（実機確認）を挟むこと。**

---

## §6. 実装AIへの共通指示（全タスクの前提として毎回添付すること）

```
あなたは index.html 1ファイルのスマホ向けライブ壁紙
「撥水ガラスに降る雨」の実装を担当する。

守ること:
- index.html 単一ファイル。外部ライブラリ・CDN・ビルド工程は禁止。
- 既存機能（設定パネル、背景画像変更、Page Visibility による停止、
  空間グリッド衝突、体積保存合体）を壊さない。
- 水滴に「水色の塗りつぶし」を使わない。水は無色透明で、見えるのは
  背景の屈折像・暗いリム・白いハイライトだけ。
- 1フレームの JS 実行時間 8ms 以内を維持する。
- 変更は指示されたタスクの範囲のみ。ついでのリファクタリング禁止。
- 完了時に、タスクに書かれた受け入れ基準を一つずつ検証し、
  結果を報告すること。
```

---

## §7. タスク指令群

各タスクは「目的 / 指示 / 受け入れ基準 / テスト手順」で構成。そのまま実装AIに渡せる。

---

### P0-1: デバッグHUDとテスト背景の追加

**目的**: 以後の全タスクの品質を計測可能にする。

**指示**:
1. URLに `#debug` が付いている場合のみ有効になるデバッグHUDを追加する。表示内容:
   - FPS（直近60フレーム移動平均）
   - フレームJS時間 ms（`performance.now()` でフレーム関数の開始〜終了を計測、直近60平均と最大値）
   - 追跡水滴数 / トレイル数
2. デバッグHUD有効時のみ、設定パネルに「テスト背景」ボタンを追加する。押すたびに背景を
   (a)通常 → (b)白黒ストライプ（縦縞、幅20px） → (c)市松模様（40px格子） → (d)真っ白 → (a)… と切替える。
   ストライプ・市松は屈折の歪みを目視確認するためのもの。
3. デバッグHUD無効時（通常URL）には一切のオーバーヘッドを発生させないこと。

**受け入れ基準**:
- `index.html#debug` で開くとHUDが出て、通常URLでは出ない。
- ストライプ背景で水滴内の縞の歪みが目視できる。
- HUD有効でもFPSが低下しない（±2fps以内）。

**テスト手順**: `#debug` 付きで開き、豪雨設定にして60秒放置。FPS・ms最大値を記録し報告する。

---

### P1-1: 屈折の非線形化（レンズらしい歪み）

**目的**: Q1。v0 の一様 `scale(-1.65)` を「中心は素通し寄り・縁ほど強く圧縮」の球面レンズ近似に置き換え、ガラス玉のような実在感を出す。

**指示**:
1. 追跡水滴（r ≥ 5.5px）の描画で、背景の屈折像を**同心リング状の多段描画**で近似する:
   クリップした円の内側を、中心側から縁側へ3〜4段のリング領域に分け、
   各段で背景の反転スケール倍率を段階的に変える（中心: 約-1.2倍 → 最外段: 約-2.2倍）。
   各段はリング状クリップ（外円パス+内円パスの evenodd）で塗り分ける。
   ※実装方式は任意だが、Canvas 2D の範囲で「縁ほど圧縮」が視覚的に成立していればよい。
   段間の境界が線として目立つ場合は、段数を増やすか各段の境界半径をぼかし気味に重ねること。
2. 1の多段描画は r ≥ 10px の水滴のみに適用する（それ未満は従来の単段でよい。性能予算のため）。
3. 落下中の楕円変形時も破綻しないこと（楕円クリップに追従）。

**受け入れ基準**:
- ストライプ背景（P0-1）で、大粒の中心の縞は太く、縁の縞は細く圧縮されて見える。
- 倒立像であること（縞の位相が背景と逆に動く）。
- 豪雨設定でFPS 55以上を維持。

**テスト手順**: `#debug` + ストライプ背景でスクショを撮り、縁部圧縮を確認。FPS報告。

---

### P1-2: 暗リムとハイライトの光学リメイク

**目的**: Q2, Q3。「縁の暗い輪」と「上寄りの小さくシャープな白ハイライト」で水の質感を決める。

**指示**:
1. 暗リム: 現行のエッジ暗化グラデーション（半径50%→100%で暗く）を、
   **縁に集中した細い帯**（半径 85%→100% の範囲だけ暗く、最大濃度 0.35 程度）に変更する。
   内側 85% はクリアに保つ。
2. ハイライト: 現行の大きな三日月グラデーションを廃止し、
   **小さい楕円形の白ハイライト**に置き換える。仕様:
   - 位置: 粒の中心から上方向（画面上方）に 0.45r、大きさ: 長径 0.35r × 短径 0.22r 程度
   - 中心はほぼ白飛び（alpha 0.85）、縁は急峻に減衰（にじませない）
   - 全ての粒で同じ方向（光源は空、つまり常に上）
   - 落下中の楕円変形時はハイライトも一緒に変形・追従する
3. 副ハイライト（任意）: 下側の縁の内側、輪郭に沿った細い弱い明るさ（全反射の内面反射光）。alpha 0.10 程度。うるさくなるなら省略可。

**受け入れ基準**:
- スクショ拡大で、リムが「輪郭に沿う細い暗環」になっている（内側までグラデが侵食していない）。
- ハイライトが小さくシャープで、全粒とも上寄りの同方向。
- 白背景・暗背景の両方で水滴が視認できる。

**テスト手順**: 白/暗/写真背景の3種でスクショを撮り比較報告。

---

### P1-3: 小滴レイヤーの水質感化

**目的**: Q4。画面の大半を占める小滴（`#small` レイヤー、r < 5.5px）が「水色の点」に見える問題を解消する。**§0-1 合格の最重要タスク。**

**指示**:
1. `drawSmallDrop()` の単色塗り（`rgba(205,228,246,…)`）を廃止し、以下の合成に置き換える:
   - 本体: 背景をわずかに暗くする半透明の暗色（例: `rgba(30,50,70,0.18)`）の円
     ＝「小滴は背景を歪めて少し暗く見える」の近似
   - 中央: ごく小さい明点（r×0.3、白 alpha 0.5）＝レンズ集光の近似
   - 輪郭: 現行の細い暗ストローク（維持）
2. 背景が暗い場合（体感で判断せず、`bgReady` 時は背景画像の平均輝度を `updateBgCvs()` で1回だけ算出して分岐）は、
   本体の暗色を明色（白 alpha 0.15 程度）に反転させ、暗背景でも視認できるようにする。
3. `#small` レイヤーの経年フェード（destination-in）は現状維持。ただし色ではなく質感が変わったことで
   視認性が変わるため、フェード係数は目視で再調整してよい（小滴が3〜10秒程度残るのが目安）。

**受け入れ基準**:
- 縞背景・写真背景・白背景いずれでも、小滴が「青い点」ではなく「表面の粒感」に見える。
- 5秒スクショテスト（§0-1）を白背景で実施し、水滴と認識できる。
- 豪雨でFPS 55以上。

**テスト手順**: 4種の背景でスクショ。before/after 比較で報告。

---

### P1-4: 接地影と集光スポット

**目的**: Q11。明るい背景での立体感の仕上げ。

**指示**:
1. 追跡水滴（r ≥ 10px のみ）の描画の最初（屈折像より先）に:
   - 粒の輪郭の外側 1.05r〜1.25r にごく薄い暗環（alpha 0.06〜0.10）＝粒がレンズとして周囲から光を奪う近似
2. 屈折像の描画後に:
   - 粒の中心やや下（+0.15r）に、半径 0.35r の柔らかい明スポット（白 alpha 0.12）＝集光
3. どちらも「言われれば分かる」程度の弱さに留める。強くしすぎて発光して見えたら失敗。

**受け入れ基準**: 白背景のスクショで、粒の周囲がわずかに暗く・中央下がわずかに明るい。写真背景で不自然な発光がない。

**テスト手順**: 白背景・写真背景でスクショ比較。

---

### P2-1: 合体ぷるぷる振動

**目的**: Q5。合体の快感の核。

**指示**:
1. `Droplet` に減衰振動状態を追加する。`absorb()` 実行時に振動を開始する:
   - 振幅の初期値: 吸収した体積比に応じる（大きい粒を食うほど大揺れ）。上限 0.25（=25%変形）
   - 周波数: 大粒ほど遅い。目安として r=10px で周期 ≈ 18フレーム、r=20px で ≈ 28フレーム（f ∝ 1/√r³ の傾向が出ていればよい）
   - 減衰: 実時間 0.15〜0.3 秒で収束（指数減衰）
2. 振動は描画スケールに適用: x方向 `1+A·sin(φ)`、y方向 `1−A·sin(φ)`（逆位相＝体積保存に見える）。
   当たり判定には反映しなくてよい（見た目のみ）。
3. 既存の `bump`（単発の膨らみ）はこの振動に統合・置換してよい。
4. 静止粒・落下粒の両方で動作すること。落下中の扁平変形とは乗算で合成する。

**受け入れ基準**:
- タップで粒を密集生成 → 合体の瞬間にぷるぷるが見える。
- 大粒の合体は「ゆっさゆっさ」、小粒は「ぷるっ」と周期差がある。
- 0.3秒以内に収束し、永久に揺れ続ける粒がいない。

**テスト手順**: タップ連打で合体を多発させ、目視確認。可能ならスロー録画。

---

### P2-2: 発進の前兆と質量感

**目的**: Q7, Q9。「そろそろ落ちるぞ…」の予測ゲームを成立させる。

**指示**:
1. **前兆**: 静止粒が臨界サイズの 85% を超えたら、以下を徐々に強める:
   - 形状を下方向にわずかに伸ばす（洋梨化）。臨界直前で縦横比 1.06 程度まで
   - ごく低速の微振動（振幅 0.5px 以下のゆらぎ。P2-1 の振動系を低振幅で流用してよい）
2. **発進**: 臨界を超えた瞬間の初速ジャーク（実装済み）は維持。加えて発進直後 5 フレームは
   洋梨形状から通常の扁平形状へ滑らかに遷移させる。
3. **質量感**: 大粒ほど加速がゆっくりになるよう、加速度に質量補正を入れる
   （同じ力なら a ∝ 1/r³ だが、実際の見た目は「大粒は最高速が速いが加速はもったり」。
   現行の vMax 二次スケーリングは維持し、加速度側だけ大粒で 0.7 倍程度に落とす）。

**受け入れ基準**:
- 3分観察で「膨らんできた粒が落ちそう」と予測でき、実際に落ちる。
- 発進が「ストン」であり「ぬるっ」ではない。
- 大粒と小粒の走りに質量差を感じる。

**テスト手順**: 中雨で3分放置し目視。前兆→発進のシーケンスを5回以上確認。

---

### P2-3: 撥水式の掃除表現への転換（トレイル大改修）

**目的**: Q6。現行の「かまぼこ水柱が残って上から消える」は親水ガラスの表現。撥水ガラスでは**跡がほぼ残らず、道がスッと綺麗になる**のが気持ちよさの核心なので、表現を転換する。

**指示**:
1. 走行中の粒の直後に見える「短い尻尾」だけを残す:
   - 現行のライブトレイル描画を、**粒の後方 最大 2.5r まで**の短い尾に制限する（それより上の古い区間は描画しない）。
   - 尾は粒側が太く、端に向かって細くなるテーパー形状。透明度も端に向かって減衰。
2. 走行経路の**掃除**: 粒が通過した帯（幅 ≈ 粒の直径）の `#small` レイヤーを消去する
   （smallCtx に destination-out で通過区間を描く）。これで「拭いた道」ができる。
3. 残留物は2種のみ:
   - **サテライト滴**: 通過帯の両脇（±0.7r〜1.2r）に確率 15% 程度で微小滴（r 0.5〜1.5px）を `drawSmallDrop`
   - **千切れビーズ**: 粒が減速したとき（スティック区間）のみ、尻から確率的に 1 個ビーズ（r 1〜2.5px）を落とす
4. 死亡時に生成される `Trail` オブジェクト（上端から消費される水柱）は**廃止**する。
   関連する `trails` 配列・`maxTrails`・`trailConsumeSpeed` 等も除去し、コードを整理する。
5. 画面下端に到達した粒は現行どおり消滅。

**受け入れ基準**:
- 大粒が走ると、通り道の小滴が消えて「拭いた帯」が見える。
- 帯の脇に微小滴が点々と残る。
- 走行後に「水柱」が残らない。
- 3分放置で「散らかる→掃除される」循環が感じられる。

**テスト手順**: 小滴を溜めてから大粒をタップ生成し、掃除の見た目を確認。3分放置テスト。

---

### P2-4: 蛇行と引き寄せ合体

**目的**: Q8。走行の直線臭さを消す。

**指示**:
1. 走行中の粒に、ピン止め不均一による**緩い蛇行**を加える: 低周波ノイズ（sin の重ね合わせ等）で
   vx に小さな揺らぎを与える。振幅は経路が ±3〜6px 揺れる程度。周期はランダムで粒ごとに違うこと。
2. **引き寄せ**: 走行中の粒の進行方向前方（下方 4r 以内・左右 2r 以内）に静止粒がある場合、
   その方向へ vx を弱く補正する（吸い寄せられて食うように見える）。最も近い1粒のみ対象。
3. 蛇行・引き寄せとも、過剰にならないこと。ジグザグ走行やホーミングミサイルに見えたら失敗。

**受け入れ基準**:
- 走行経路が完全な直線でない。
- 時々、粒が少し横にそれて静止粒を食うのが見える。
- 不自然な急カーブがない。

**テスト手順**: 大雨で2分観察。走行10回のうち数回で進路変更が確認できること。

---

### P2-5: 小滴生態系（静止小滴の接触合体と成長）

**目的**: Q10。放置時の「画面が生きている」感。

**指示**:
1. `#small` レイヤーの小滴は描き捨てで追跡されていないため、**確率的な成長イベント**で近似する:
   毎フレーム低確率で、過去に小滴を描いた位置の近傍に「少し大きい滴が生まれ、周囲の小滴が消える」
   演出を行う。実装方式は任意。例:
   - 小滴の描画位置を軽量なリングバッファ（座標+半径のみ、最大数百件）で記録
   - 毎フレーム数件サンプリングし、距離 6px 以内のペアがあれば両方を destination-out で消し、
     中間点に合体後サイズ（面積和相当）の滴を描く
   - 合体後サイズが追跡閾値（5.5px）を超えたら、追跡 `Droplet` として昇格生成する
2. 効果は控えめでよい。5分放置で「小滴が育って、時々追跡粒に昇格し、やがて落ちる」循環が
   1〜2回観測できる程度の頻度に調整する。
3. 性能: この処理は1フレームあたり 0.5ms 以内。重ければ処理を数フレームに分散する。

**受け入れ基準**:
- 5分放置して、小滴の合体成長→昇格→落下→掃除 の一連が観測できる。
- FPS低下なし。

**テスト手順**: `#debug` で5分放置し、循環の発生とFPSを報告。

---

### P3-1〜P3-4: WebGL化（P1・P2 合格後にのみ着手。着手前にユーザー承認を得ること）

> ここは方向性のみ記す。着手時に P3 詳細仕様を別途起こすこと（その時点の実装状態に依存するため）。

- **P3-1 基盤**: WebGL コンテキスト＋フルスクリーンクアッド＋背景テクスチャ描画。WebGL 不可端末は現行 Canvas 2D に自動フォールバック。
- **P3-2 法線マップ屈折**: 水滴群を「高さ・法線バッファ」としてオフスクリーンに描き、フラグメントシェーダで背景UVを法線に応じてオフセットサンプリング（真の屈折）。Fresnel でリム反射。
- **P3-3 小滴大量化**: 静的小滴をインスタンシングで数千個描画。生態系（P2-5）をGPU側の実体として持ち直す。
- **P3-4 品質切替**: 端末性能に応じ Canvas2D / WebGL-low / WebGL-high を自動選択＋設定で手動切替。

---

### P4-1: 実機チューニングと省電力仕上げ

**目的**: Q12, Q13 の最終確認と、ライブ壁紙としての実用性。

**指示**:
1. `devicePixelRatio` 対応の見直し: 高DPI端末でぼやけない範囲で、内部解像度に上限（例: 長辺 1600px）を設け、超過時はスケール描画にする。
2. 設定パネルに「省電力モード」トグルを追加: ON で目標 30fps に制限（rAF を1回おきに処理）し、雨条(streaks)を無効化。
3. アイドル最適化: 追跡粒が0個かつ降雨強度が最低のとき、描画更新間隔を自動で落とす。
4. 3分・豪雨・写真背景の連続動作で、フレーム時間の p95 を計測して報告する。

**受け入れ基準**:
- 豪雨で p95 フレーム時間 12ms 以下（デスクトップ計測）。
- 省電力モードで見た目が破綻しない。
- 画面OFF→ONの復帰が1秒以内で、復帰時に粒が瞬間移動しない。

**テスト手順**: `#debug` HUD で各モードを計測し数値報告。

---

## §8. テスト計画（横断）

### 8.1 各タスク共通のリグレッションチェック（実装AIは毎タスク終了時に実施）

1. 通常URL（非debug）で開いて JS エラーがコンソールにないこと。
2. 設定パネル: サイズ2スライダー・降雨強度・背景変更が全て機能すること。
3. タップで粒が生成され、近傍の粒が合体すること。
4. タブを非表示→再表示して描画が再開すること（粒がワープしないこと）。
5. ウィンドウリサイズで破綻しないこと。
6. `#debug` 豪雨60秒で FPS 55 以上。

### 8.2 マイルストーンレビュー（ユーザー実施）

- **P1完了時**: 実機（スマホ）で §0 判定基準 1（水に見えるか）を判定。縞背景スクショを添付。
- **P2完了時**: 実機で §0 判定基準 2・3（撥水に見えるか・飽きないか）を3分視聴で判定。
- **P4完了時**: §4 注目表の全項目を採点した表を提出。

### 8.3 スクリーンショット比較の運用

- 各タスクの前後で同条件（同背景・同強度・起動30秒後）のスクショを撮り、コミットメッセージまたは報告に before/after として添付する。
- 基準背景: (1)白 (2)縦縞20px (3)風景写真 の3種。

---

## §9. 用語集

| 用語 | 意味 |
|---|---|
| 追跡水滴 | `Droplet` クラスのインスタンスとして物理計算される粒（r ≥ 5.5px） |
| 小滴レイヤー | `#small` キャンバス。描き捨ての微小滴。destination-in でフェード |
| 球帽 (spherical cap) | 球を平面で切った形。撥水面上の静止水滴の形 |
| 接触角ヒステリシス | 前進角と後退角の差。小さいほど転がりやすく跡が残らない |
| サテライト滴 | 走行する粒の経路脇に弾き残される微小滴 |
| 掃除 (sweep) | 走行粒が経路上の小滴を吸収し、道が綺麗になる現象 |
| 昇格 | 小滴レイヤー上の滴が閾値を超え、追跡水滴として生成し直されること |
| HUD | `#debug` 時のみ表示される計測オーバーレイ |
